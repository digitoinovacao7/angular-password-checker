export enum ErrorMessage {
  invalidLength = 'Password must be at least 8 characters.',
  whitespaces = 'Password must not contains a witespaces.',
  doesNotMatchPattern = 'Password must contains digits, letters, special characters.',
}
